<%= form_for(@author, method: :delete) do |f| %>
    <p>Are you sure you want to delete this author permanently?</p>
    <h2>
      <%= f.object.first_name %> <%= f.object.last_name %>
    </h2>

    <%= f.submit 'Delete', class: 'btn btn-danger' %>
<% end %>